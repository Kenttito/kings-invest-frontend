import React from 'react';
import { BrowserRouter, Routes, Route } from 'react-router-dom';

function App() {
  return (
    <BrowserRouter>
      <Routes>
        {/* Add your routes here when ready to rebuild admin flow */}
      </Routes>
    </BrowserRouter>
  );
}

export default App; 